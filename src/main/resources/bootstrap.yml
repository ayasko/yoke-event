spring:
  application:
    name: yoke-event
    
  cloud:
    config:
      discovery:
        enabled: true
        service-id: yoke-configserver
      
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8580/eureka